/*import { createAction, props } from "@ngrx/store";
import { Meal } from "src/app/models/meal";

const repas: string = '[Repas] ';

export const Clear = createAction(repas + 'Clear all');
export const AddOne = createAction(repas + 'Add', props<{payload: Meal}>());
export const AddMany = createAction(repas + 'Add many', props<{payload: Meal[]}>());
*/