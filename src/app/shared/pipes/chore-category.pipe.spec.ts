import { ChoreCategoryPipe } from './chore-category.pipe';

describe('ChoreCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new ChoreCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
